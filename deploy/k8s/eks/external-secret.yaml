# External Secrets Operator (ESO) — sync dari AWS Secrets Manager → K8s Secret.
#
# Prerequisite:
#   1. ESO installed di cluster (lihat aws-eks-architecture.md §5.2)
#   2. ClusterSecretStore "aws-secrets-manager" sudah dikonfigurasi
#      dengan IRSA pointing ke role yang bisa read ajipur-parkir-pintar/* paths
#   3. Secrets di AWS Secrets Manager sudah dibuat:
#      - ajipur-parkir-pintar/db-url
#      - ajipur-parkir-pintar/midtrans-server-key
#      - ajipur-parkir-pintar/jwt-secret
#
# Apply:
#   kubectl apply -f deploy/k8s/eks/external-secret.yaml -n parkir
#
# Result: K8s Secret "parkir-secrets" akan otomatis populate + auto-refresh
# tiap 1 jam. Helm chart reference Secret ini via secretKeyRef.

---
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aws-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-southeast-3
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets-sa
            namespace: external-secrets
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: parkir-secrets
  namespace: parkir
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: parkir-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/part-of: parkir-pintar
  data:
    - secretKey: db-url
      remoteRef:
        key: ajipur-parkir-pintar/db-url
    - secretKey: midtrans-server-key
      remoteRef:
        key: ajipur-parkir-pintar/midtrans-server-key
    - secretKey: jwt-secret
      remoteRef:
        key: ajipur-parkir-pintar/jwt-secret
    # Dev-token endpoint gating. Kalau secret ada di Secrets Manager + nilainya
    # non-empty, dev-token endpoint aktif. Postman client harus include
    # `X-Dev-Secret: <value>` header. Production: jangan create secret ini.
    - secretKey: dev-token-secret
      remoteRef:
        key: ajipur-parkir-pintar/dev-token-secret
